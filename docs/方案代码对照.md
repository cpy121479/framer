# 平台方案 ↔ 代码对照

> 用途：把方案文档（`uvm_poe平台方案.md`、`数据结构位宽总表.md`）与
> `uvm_poe/` 工程代码一一对应，便于按方案检查代码、按代码回溯方案。
> 状态标注：✅ 已实现 / 🟡 模型占位（方案已定义，代码简化或未实现）。

## 0. 总体映射

| 方案内容 | 对应代码 |
| --- | --- |
| §2 KOA 输入激励（时隙表/速率模型） | `uvm_poe/uvm/seq/*`（koa_oh_seq / koa_x2x_seq / koa_uart_seq） |
| §3 KOA 输入仲裁 + 8 组 RR+SP | `uvm_poe/rtl/koa.sv` |
| §4 THM（线程管理/保序/CSR/pre 插队） | `uvm_poe/rtl/poe_thm.sv` |
| §5 th_sch（一级发射） | `uvm_poe/rtl/poe_thsch.sv` |
| §6 burst_sch（二级发射）+ C 窗结构 | `uvm_poe/rtl/poe_burstsch.sv` + `poe_dma_ctrl_stub.sv` |
| §7 CU/EU 桩、dma_ctrl 桩 | `uvm_poe/rtl/poe_cu_stub.sv`、`poe_dma_ctrl_stub.sv` |
| 数据结构位宽总表 | `uvm_poe/rtl/poe_types.sv`（burst_t / csr_t / cw_entry_t / c_wnd_entry_t） |
| §9 验证 | `uvm_poe/tb/tb_top.sv`（链路日志）+ `uvm/scoreboard/koa_scoreboard.sv` |
| 运行/参数 | `uvm_poe/sim/run_verilator.sh`、各 RTL parameter |

## 1. 模块对照

| 方案章节/内容 | 代码文件 | 关键实现 | 状态 |
| --- | --- | --- | --- |
| §3 输入仲裁（7 流优先级，每拍选 1 条） | `rtl/koa.sv` | `always_comb` 仲裁（w_vld/w_stream/w_pri/w_sel_plane） | ✅ |
| §3 8 组优先级队列 + SP/FIFO 出队 | `rtl/koa.sv` | `q_mem[8][16]`、`sel_grp`、`q_cnt_n` 合并计数（同拍写读净 0）、输出寄存一拍 | ✅ |
| §3 输入反压（per-stream 选通） | `rtl/koa.sv` | 仅仲裁选中的流/平面 rdy=1，未选中流 vld 保持 → 不丢 | ✅ |
| §3 接收确认（ack） | `rtl/koa.sv` | `ack_vld/stream/pri/data/cid/pos`（入队拍寄存输出，供 monitor/scoreboard） | ✅ |
| §4 保序（同 key 活跃→缓存） | `rtl/poe_thm.sv` | `key_active()`、`buf_mem`（8 深）、`can_accept/ko_rdy` | ✅ |
| §4 线程状态机 + bs_pc/cur_ts 解耦 | `rtl/poe_thm.sv` | `th_state`（4 态）、`th_bs_pc` 打拍推进、`cu_done/dma_done` 统计 `th_cur_ts` | ✅ |
| §4 一级发射打拍（branch 1+3+t） | `rtl/poe_thm.sv` | `iss_vld0/1` → ISSUED + `th_wait` | ✅ |
| §4 CSR 表项（建线程生成/同步） | `rtl/poe_thm.sv` | `csr[]`（csr_t）、th_stat/cur_ts 同步、`csr_dma_c/csr_cw` 暴露 | ✅ |
| §4 pre_read 插队 | `rtl/poe_thm.sv` + `poe_thsch.sv` | `ko_pre_read` → `pre_inj_*`（队列项 pre 标志区分） | 🟡 单路占位（方案 4 组） |
| §4 线程结束通知 | `rtl/poe_thm.sv` | `th_rel_vld/th_rel_tid`（T_DONE 拍） | ✅ |
| §5 (pri,tid) 选 ≤2 | `rtl/poe_thsch.sv` | 两级最小选择（iss0/iss1） | ✅ |
| §5 burst 队列（2×8 深） | `rtl/poe_thsch.sv` | 队列项 {pre, burst, burst_ts, th_id} = 41bit | ✅ |
| §5 pre 注入（q0 优先） | `rtl/poe_thsch.sv` | `pre_inj_vld/rdy`、`pre_target0/1` | ✅ |
| §6 公共发射条件 | `rtl/poe_burstsch.sv` | `pre \|\| ts==cur_ts` + 无 O 窗反压（tr） | ✅ |
| §6 c_task 条件（c0/c1 + dma_c） | `rtl/poe_burstsch.sv` | `need`（c0&dma_c 计数）、资源池条件 | ✅ |
| §6 发射路由（i/v→CU，c→dma） | `rtl/poe_burstsch.sv` | `emit_cu_*` / `emit_dma_*`（+pre 标志） | ✅ |
| §6 C 窗资源池（256 深 FIFO） | `rtl/poe_dma_ctrl_stub.sv` | `f_mem/f_cnt/f_head/f_tail` | ✅ |
| §6 资源申请/归还（dma_done+线程兜底） | `rtl/poe_dma_ctrl_stub.sv` | `alloc_n/alloc_cnt_r/done_free_n/rel_free_n`、`th_res_n_r` | ✅ |
| §7.1 CU 桩 | `rtl/poe_cu_stub.sv` | LATENCY 后回 `cu_done` | 🟡 vtsk_c 判定未实现 |
| §7.2 dma_ctrl 资源管理 | `rtl/poe_dma_ctrl_stub.sv` | 申请/归还/上限 4、pre 不占资源 | ✅ |
| §7.2 loc/free 完整流程 | `rtl/poe_dma_ctrl_stub.sv` | 仅头部注释描述（3 拍查 C 窗/指令预存/RBA/SMC） | 🟡 占位 |

## 2. 数据结构对照（位宽总表 ↔ poe_types.sv）

| 数据结构 | 位宽表章节 | poe_types.sv | 使用处 |
| --- | --- | --- | --- |
| burst（一种结构两种类型） | §2 | `burst_iv_t` / `burst_c_t`（可互 cast） | THM `th_burst_r`、th_sch 队列、burst_sch 解码 |
| CSR 表项 | §4 | `csr_t`（th_id 6bit、cw 8×6B） | THM `csr[]`、`csr_dma_c/csr_cw` 暴露 |
| CSR.cw 条目（48bit） | §4.1 | `cw_entry_t` | dma_ctrl 按 dma_id 查询（🟡 未写条目内容） |
| C 窗资源条目（168bit） | §5.1 | `c_wnd_entry_t` | 🟡 仅类型定义，未实际写入 |
| 操作指令（28bit×4 路/拍） | §7 | —（未定义类型） | 🟡 burst 粒度传递，未拆 4 路接口 |
| 指令预存资源（36bit×16） | §6 | — | 🟡 未实现 |
| pre_mes（28bit×4 组） | §12/§15 | — | 🟡 模型单路注入 |
| 保序键（stream/cid/pos） | §11 | —（端口位宽） | THM `key_active()` |
| th_sch 队列项（41bit） | §3 | —（QW localparam） | `poe_thsch.sv` |

## 3. 处理流程对照

| 流程 | 方案位置 | 代码位置 | 状态 |
| --- | --- | --- | --- |
| 线程建/释放（保序放行） | §4.1-4.2 | `poe_thm.sv` 创建块、释放循环 | ✅ |
| 一级发射（READY→ISSUED→打拍→READY） | §4.3-4.4 | `poe_thm.sv` issue/wait 块 | ✅ |
| cur_ts 推进（cu_done/dma_done 统计） | §4.5 | `poe_thm.sv` done 块 | ✅ |
| 二级发射判定 + 操作指令生成 | §6.2-6.3 | `poe_burstsch.sv` 条件/路由块 | ✅（指令接口占位） |
| C 窗申请/归还（FIFO） | §6 附 | `poe_dma_ctrl_stub.sv` 申请/归还块 | ✅ |
| pre 插队（不建线程/不查保序） | §4.6 | `poe_thm.sv` pre_inj 赋值 | ✅ |
| loc 操作（查 C 窗/申请/更新 cw/RBA 读） | §7.2 | 占位（注释） | 🟡 |
| free 操作（去重/RBA 写/释放/转交） | §7.2 | 占位（注释） | 🟡 |

## 4. 激励与验证对照

| 内容 | 代码 |
| --- | --- |
| 激励（3 业务源并发） | 3 个 sequencer+driver：fgOTN（oh_e/oh_i）、X2X（aps_e/aps_i/alm）、串口（u_e/u_i）；同源共享 sequencer（item 串行），跨源并发（最大 3 路） |
| 线程描述旁路（burst 模式 / vtsk_c / dma_c / cw 随机） | `tb/tb_top.sv`（`rand_burst_iv/c/seq` + 线程描述块） |
| pre_read 标志（1/16 概率） | `tb/tb_top.sv` |
| 链路事件日志（ISS/EMIT/DONE/CBLOCK/OLD_BURST/FINAL） | `tb/tb_top.sv`（thm_dbg.log） |
| KOA scoreboard（8 组参考模型；同刻 OUT 先于 IN 处理，复现 KOA 先读后写） | `uvm/scoreboard/koa_scoreboard.sv` |
| 编译/运行（参数） | `sim/run_verilator.sh`、`filelist.f` |

## 5. 占位 / 未实现清单（对应方案 §10）

| 项 | 现状 |
| --- | --- |
| dma_ctrl loc/free 完整流程 | 占位（注释描述），仅资源池管理已实现 |
| 指令预存资源（16 深） | 未实现 |
| RBA 总线 / SMC 缓存 | 未实现 |
| 操作指令显式 4 路接口 | 未拆（burst 粒度传递） |
| pre_mes 4 组 | 模型单路占位 |
| CU 的 vtsk_c 任务执行判定 | 未实现（桩直接完成） |
| I_BUF_B 子指令 / EU（mux/inst_b/dmux） | 未实现 |
| O 窗资源池（owin_bp） | 外部固定 0 |
| CSR ccr / tw / o_mes / err 刷新 | 占位 |
| C 窗资源条目内容写入 | 仅类型定义 |
